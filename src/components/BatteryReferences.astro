---
import { Image } from 'astro:assets';
import { references, siteData } from '../data/siteData';
---

<section class="py-20 bg-slate-800">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <div class="inline-flex items-center gap-2 bg-yellow-500/10 border border-yellow-500/30 px-4 py-2 rounded-full mb-4">
        <i class="ri-building-line text-yellow-500"></i>
        <span class="text-yellow-500 font-bold text-sm">주요 설치 레퍼런스</span>
      </div>
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-4">신뢰할 수 있는 파트너</h2>
      <p class="text-xl text-gray-300 max-w-2xl mx-auto">
        은행, 공공기관, 병원, 대기업 등 신뢰할 수 있는 파트너와 함께합니다
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {references.map((ref) => (
        <div class="group bg-slate-900 rounded-xl overflow-hidden border border-slate-700 hover:border-yellow-500 transition-all transform hover:scale-105">
          <div class="relative h-48 overflow-hidden">
            <Image
              src={ref.image}
              alt={ref.title}
              width={400}
              height={300}
              class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500"
              loading="lazy"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-slate-900 to-transparent"></div>
            <div class="absolute top-4 left-4">
              <div class="w-12 h-12 bg-yellow-500 rounded-lg flex items-center justify-center">
                <i class={`${ref.icon} text-2xl text-slate-900`}></i>
              </div>
            </div>
          </div>
          <div class="p-6">
            <h3 class="text-xl font-bold text-white mb-2">{ref.title}</h3>
            <p class="text-gray-300 text-sm">{ref.description}</p>
          </div>
        </div>
      ))}
    </div>

    <div class="mt-16 text-center">
      <div class="bg-slate-900 p-8 rounded-2xl border border-slate-700 inline-block">
        <p class="text-gray-300 mb-4">B2B 제휴 및 대량 납품 문의</p>
        <a
          href={`tel:${siteData.phone}`}
          class="inline-flex items-center gap-3 bg-yellow-500 hover:bg-yellow-400 text-slate-900 px-8 py-4 rounded-lg font-bold text-lg transition-all transform hover:scale-105"
        >
          <i class="ri-briefcase-line text-xl"></i>
          제휴 문의하기
        </a>
      </div>
    </div>
  </div>
</section>
