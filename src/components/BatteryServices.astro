---
import { Image } from 'astro:assets';
import { services, siteData } from '../data/siteData';
---

<section id="services" class="py-20 bg-slate-800">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <div class="inline-flex items-center gap-2 bg-yellow-500/10 border border-yellow-500/30 px-4 py-2 rounded-full mb-4">
        <span class="text-yellow-500 font-bold text-sm">산업 현장의 전력 파트너</span>
      </div>
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-4">전문 서비스</h2>
      <p class="text-xl text-gray-300 max-w-2xl mx-auto">
        업계 최다 설치사례와 전문 지식으로 다운타임 ZERO를 보장합니다
      </p>
    </div>

    <div class="space-y-12">
      {services.map((service, index) => (
        <div class={`group bg-slate-900 rounded-2xl border border-slate-700 overflow-hidden hover:border-yellow-500 transition-all ${index % 2 === 1 ? 'lg:flex-row-reverse' : ''}`}>
          <div class="grid grid-cols-1 lg:grid-cols-2 gap-0">
            <div class="relative h-80 lg:h-auto overflow-hidden">
              <Image
                src={service.image}
                alt={service.title}
                width={800}
                height={600}
                class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                loading="lazy"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-slate-900 via-slate-900/50 to-transparent"></div>
              <div class="absolute top-6 left-6">
                <span class="bg-yellow-500 text-slate-900 text-sm px-4 py-2 rounded-full font-bold">
                  {service.category}
                </span>
              </div>
            </div>

            <div class="p-8 lg:p-12 flex flex-col justify-center">
              <div class="flex items-center gap-4 mb-6">
                <div class="w-16 h-16 bg-yellow-500/10 rounded-xl flex items-center justify-center shrink-0">
                  <i class={`${service.icon} text-4xl text-yellow-500`}></i>
                </div>
                <h3 class="text-3xl font-bold text-white">{service.title}</h3>
              </div>

              <p class="text-gray-300 text-lg mb-8 leading-relaxed">
                {service.description}
              </p>

              <div class="grid grid-cols-2 gap-4 mb-8">
                {service.features.map((feature) => (
                  <div class="flex items-center gap-3 text-gray-300">
                    <i class="ri-checkbox-circle-fill text-yellow-500 text-xl shrink-0"></i>
                    <span>{feature}</span>
                  </div>
                ))}
              </div>

              <a
                href={`tel:${siteData.phone}`}
                class="inline-flex items-center gap-2 text-yellow-500 hover:text-yellow-400 font-semibold text-lg group-hover:gap-4 transition-all"
              >
                지금 상담받기
                <i class="ri-arrow-right-line text-xl"></i>
              </a>
            </div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

