---
import { testimonials } from '../data/siteData';
---

<section class="py-20 bg-slate-900">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <div class="inline-flex items-center gap-2 bg-yellow-500/10 border border-yellow-500/30 px-4 py-2 rounded-full mb-4">
        <i class="ri-chat-quote-line text-yellow-500"></i>
        <span class="text-yellow-500 font-bold text-sm">고객 후기</span>
      </div>
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-4">고객님의 목소리</h2>
      <p class="text-xl text-gray-300 max-w-2xl mx-auto">
        실제 고객들의 생생한 후기를 확인해보세요
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-6xl mx-auto">
      {testimonials.map((testimonial) => (
        <div class="bg-slate-800 p-8 rounded-2xl border border-slate-700 hover:border-yellow-500 transition-all">
          <div class="flex mb-6 gap-1">
            {Array.from({ length: testimonial.rating }).map(() => (
              <i class="ri-star-fill text-yellow-500 text-xl"></i>
            ))}
          </div>
          <p class="text-gray-300 mb-6 italic leading-relaxed text-lg">"{testimonial.content}"</p>
          <div class="pt-6 border-t border-slate-700">
            <div class="font-bold text-white text-lg mb-1">{testimonial.name}</div>
            <div class="text-sm text-yellow-500">{testimonial.company}</div>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>
