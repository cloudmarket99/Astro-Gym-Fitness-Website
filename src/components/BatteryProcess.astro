---
import { process, siteData } from '../data/siteData';
---

<section id="process" class="py-20 bg-slate-900">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <div class="inline-flex items-center gap-2 bg-yellow-500/10 border border-yellow-500/30 px-4 py-2 rounded-full mb-4">
        <i class="ri-route-line text-yellow-500"></i>
        <span class="text-yellow-500 font-bold text-sm">간편하고 확실한 설치 절차</span>
      </div>
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-4">설치 절차</h2>
      <p class="text-xl text-gray-300 max-w-2xl mx-auto">
        상담 → 무료진단 → 신속설치 → 사후관리까지 올인원!
      </p>
    </div>

    <div class="max-w-5xl mx-auto">
      <div class="relative">
        <div class="hidden md:block absolute top-0 left-0 right-0 h-1 bg-slate-700" style="top: 80px;"></div>
        <div class="hidden md:block absolute top-0 left-0 h-1 bg-yellow-500 transition-all" style="top: 80px; width: 75%;"></div>

        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
          {process.map((step, index) => (
            <div class="relative">
              <div class="bg-slate-800 p-6 rounded-2xl border-2 border-slate-700 hover:border-yellow-500 transition-all">
                <div class="flex flex-col items-center text-center">
                  <div class="w-20 h-20 bg-yellow-500 rounded-full flex items-center justify-center mb-4 relative z-10 shadow-lg shadow-yellow-500/20">
                    <i class={`${step.icon} text-3xl text-slate-900`}></i>
                  </div>
                  <div class="text-yellow-500 font-bold text-sm mb-2">STEP {step.step}</div>
                  <h3 class="text-xl font-bold text-white mb-3">{step.title}</h3>
                  <p class="text-gray-300 text-sm leading-relaxed">{step.description}</p>
                </div>
              </div>
              {index < process.length - 1 && (
                <div class="hidden md:block absolute top-20 -right-4 z-20">
                  <i class="ri-arrow-right-line text-2xl text-yellow-500"></i>
                </div>
              )}
            </div>
          ))}
        </div>
      </div>
    </div>

    <div class="mt-16 text-center">
      <a
        href={`tel:${siteData.phone}`}
        class="inline-flex items-center gap-3 bg-yellow-500 hover:bg-yellow-400 text-slate-900 px-10 py-5 rounded-lg font-bold text-xl transition-all transform hover:scale-105 shadow-lg shadow-yellow-500/20"
      >
        <i class="ri-phone-line text-2xl"></i>
        지금 바로 상담받기
        <i class="ri-arrow-right-line text-xl"></i>
      </a>
    </div>
  </div>
</section>

