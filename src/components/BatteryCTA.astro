---
import { siteData, specialOffers } from '../data/siteData';
---

<section class="py-20 bg-slate-900 relative overflow-hidden">
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_50%_50%,rgba(251,191,36,0.1),transparent_70%)]"></div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="bg-gradient-to-br from-yellow-500 to-yellow-600 rounded-3xl p-12 lg:p-16 text-center shadow-2xl shadow-yellow-500/20">
      <div class="inline-flex items-center gap-2 bg-slate-900/20 px-4 py-2 rounded-full mb-6">
        <i class="ri-alarm-warning-line text-white text-xl animate-pulse"></i>
        <span class="text-white font-bold text-sm">긴급 출동 잔여 슬롯 3개 남음!</span>
      </div>

      <h2 class="text-4xl md:text-5xl lg:text-6xl font-bold text-slate-900 mb-6">
        배터리 문제,<br/>지금 바로 해결하세요!
      </h2>

      <p class="text-xl md:text-2xl text-slate-800 mb-8 max-w-3xl mx-auto">
        지금 결단하시고, 경쟁사가 흉내 낼 수 없는<br/>
        <span class="font-bold">초신속 긴급 대응 시스템</span>을 확보하세요
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
        <a
          href={`tel:${siteData.phone}`}
          class="group bg-slate-900 hover:bg-slate-800 text-white px-10 py-6 rounded-xl font-bold text-xl transition-all transform hover:scale-105 inline-flex items-center justify-center gap-3 shadow-xl"
        >
          <i class="ri-phone-line text-2xl"></i>
          <div class="text-left">
            <div class="text-xs text-yellow-500">긴급 출장 전화</div>
            <div class="text-2xl">{siteData.phone}</div>
          </div>
        </a>
        <a
          href={`mailto:${siteData.email}`}
          class="bg-white hover:bg-gray-100 text-slate-900 px-10 py-6 rounded-xl font-bold text-xl transition-all inline-flex items-center justify-center gap-3 shadow-xl"
        >
          <i class="ri-mail-line text-2xl"></i>
          견적 문의
        </a>
      </div>
    </div>

    <div class="mt-16">
      <div class="text-center mb-8">
        <h3 class="text-3xl font-bold text-white mb-4">
          지금 문의 시 제공되는 <span class="text-yellow-500">3가지 보너스 혜택</span>
        </h3>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-5xl mx-auto">
        {specialOffers.map((offer, index) => (
          <div class="bg-slate-800 p-8 rounded-2xl border-2 border-yellow-500/20 hover:border-yellow-500 transition-all transform hover:scale-105">
            <div class="flex flex-col items-center text-center">
              <div class="w-16 h-16 bg-yellow-500/10 rounded-full flex items-center justify-center mb-4">
                <i class={`${offer.icon} text-3xl text-yellow-500`}></i>
              </div>
              <div class="text-yellow-500 font-bold text-sm mb-2">혜택 {index + 1}</div>
              <h4 class="text-xl font-bold text-white mb-3">{offer.title}</h4>
              <p class="text-gray-300">{offer.description}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
